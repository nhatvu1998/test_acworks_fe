import { Row } from "antd";
import styled from "styled-components";

const Content = styled.p`
  text-align: center;
`;

const StyledRow = styled(Row)`
  background-color: #fff;
  display: flex;
  align-items: center;
  justify-content: center;
  min-height: 100px;
`;

export { Content, StyledRow };
